import { Typography } from '@mui/material';

import { PageContainer } from '../components/containers/pageContainer';

export const Homepage = () => {
  return (
    <PageContainer>
      <Typography>Header</Typography>
      <Typography>SmartSus</Typography>
    </PageContainer>
  );
};
